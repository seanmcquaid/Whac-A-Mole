import React from 'react';
import ScoreStatus from '../ScoreStatus';

describe('<ScoreStatus/>', () => {
  it('Should display if score is in the top ten', () => {});

  it("Should display if score isn't the top ten", () => {});

  it('Should allow the user to enter their name and submit if their score is in the top 10', () => {});

  it("Should allow the user to go back home if their score isn't in the top 10", () => {});
});
