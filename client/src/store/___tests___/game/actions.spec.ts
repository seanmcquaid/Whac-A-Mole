import {
  hitMole,
  startGame,
  incrementTimer,
  endGame,
} from '../../game/actions';

describe('gameActions', () => {
  it('hitMole', () => {});

  it('startGame', () => {});

  it('incrementTimer', () => {});

  it('endGame', () => {});
});
